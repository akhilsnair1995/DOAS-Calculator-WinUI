<Window
    x:Class="DOASCalculatorWinUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DOASCalculatorWinUI">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="420"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <MenuBar Grid.Row="0" Grid.ColumnSpan="2">
            <MenuBarItem Title="File"><MenuFlyoutItem Text="Exit" Click="Exit_Click"/></MenuBarItem>
            <MenuBarItem Title="Units">
                <ToggleMenuFlyoutItem x:Name="MenuSI" Text="SI (Metric)" IsChecked="True" Click="Units_Click"/>
                <ToggleMenuFlyoutItem x:Name="MenuIP" Text="IP (Imperial)" Click="Units_Click"/>
            </MenuBarItem>
        </MenuBar>

        <!-- Left Pane: Inputs -->
        <Border Grid.Row="1" Grid.Column="0" BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}" BorderThickness="0,0,1,0">
            <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="24">
                <StackPanel Spacing="20">
                    <TextBlock Text="System Parameters" Style="{StaticResource SubtitleTextBlockStyle}" Margin="0,0,0,4"/>

                    <StackPanel Spacing="8">
                        <TextBlock Text="Project Context" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                        <NumberBox x:Name="NumAltitude" Header="Altitude (m)" Value="0" ValueChanged="Input_Changed" HorizontalAlignment="Stretch"/>
                    </StackPanel>

                    <StackPanel Spacing="12">
                        <TextBlock Text="Ambient Design Conditions" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                        <Grid ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <NumberBox x:Name="NumOaFlow" Grid.Column="0" Header="OA Flow (L/s)" Value="1000" ValueChanged="Input_Changed" HorizontalAlignment="Stretch"/>
                            <NumberBox x:Name="NumOaDb" Grid.Column="1" Header="OA DB (°C)" Value="35.0" ValueChanged="Input_Changed" HorizontalAlignment="Stretch"/>
                        </Grid>
                        <Grid ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <NumberBox x:Name="NumOaWb" Grid.Column="0" Header="OA WB (°C)" Value="28.0" ValueChanged="Input_Changed" HorizontalAlignment="Stretch"/>
                            <NumberBox x:Name="NumEaRh" Grid.Column="1" Header="EA RH (%)" Value="50.0" ValueChanged="Input_Changed" HorizontalAlignment="Stretch"/>
                        </Grid>
                        <Grid ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <NumberBox x:Name="NumEaFlow" Grid.Column="0" Header="EA Flow (L/s)" Value="800" ValueChanged="Input_Changed" HorizontalAlignment="Stretch"/>
                            <NumberBox x:Name="NumEaDb" Grid.Column="1" Header="EA DB (°C)" Value="24.0" ValueChanged="Input_Changed" HorizontalAlignment="Stretch"/>
                        </Grid>
                    </StackPanel>

                    <StackPanel Spacing="8">
                        <TextBlock Text="Primary Energy Recovery" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                        <ToggleSwitch x:Name="TogWheel" Header="Enthalpy Wheel" IsOn="True" Toggled="Input_Changed"/>
                        <Grid ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <NumberBox x:Name="NumWheelSens" Grid.Column="0" Header="Sensible Eff (%)" Value="75" ValueChanged="Input_Changed" HorizontalAlignment="Stretch"/>
                            <NumberBox x:Name="NumWheelLat" Grid.Column="1" Header="Latent Eff (%)" Value="70" ValueChanged="Input_Changed" HorizontalAlignment="Stretch"/>
                        </Grid>
                        <CheckBox x:Name="ChkEconomizer" Content="Enthalpy Economizer" IsChecked="True" Click="Input_Changed"/>
                    </StackPanel>

                    <StackPanel Spacing="8">
                        <TextBlock Text="Dehumidification Strategy" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                        <Grid ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <ToggleSwitch x:Name="TogDw" Grid.Column="0" Header="Dual Wheel" IsOn="False" Toggled="Input_Changed"/>
                            <ToggleSwitch x:Name="TogHp" Grid.Column="1" Header="Heat Pipe" IsOn="False" Toggled="Input_Changed"/>
                        </Grid>
                        <Grid ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <NumberBox x:Name="NumDwEff" Grid.Column="0" Header="Eff (%)" Value="65" ValueChanged="Input_Changed" HorizontalAlignment="Stretch"/>
                            <NumberBox x:Name="NumHpEff" Grid.Column="1" Header="Eff (%)" Value="45" ValueChanged="Input_Changed" HorizontalAlignment="Stretch"/>
                        </Grid>
                    </StackPanel>

                    <StackPanel Spacing="8">
                        <TextBlock Text="Conditioning and Fans" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                        <Grid ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <NumberBox x:Name="NumOffCoil" Grid.Column="0" Header="Off-Coil (°C)" Value="12.0" ValueChanged="Input_Changed" HorizontalAlignment="Stretch"/>
                            <NumberBox x:Name="NumFanEff" Grid.Column="1" Header="Fan Eff (%)" Value="60" ValueChanged="Input_Changed" HorizontalAlignment="Stretch"/>
                        </Grid>
                        <Grid ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <ToggleSwitch x:Name="TogReheat" Grid.Column="0" Header="Post-Reheat" IsOn="False" Toggled="Input_Changed"/>
                            <NumberBox x:Name="NumSupplyTemp" Grid.Column="1" Header="Supply (°C)" Value="20.0" ValueChanged="Input_Changed" HorizontalAlignment="Stretch"/>
                        </Grid>
                        <Grid ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <NumberBox x:Name="NumSupEsp" Grid.Column="0" Header="Sup ESP (Pa)" Value="500" ValueChanged="Input_Changed" HorizontalAlignment="Stretch"/>
                            <NumberBox x:Name="NumExtEsp" Grid.Column="1" Header="Ext ESP (Pa)" Value="500" ValueChanged="Input_Changed" HorizontalAlignment="Stretch"/>
                        </Grid>
                    </StackPanel>
                    
                    <Button Content="Recalculate System" Click="Calculate_Click" Style="{StaticResource AccentButtonStyle}" HorizontalAlignment="Stretch" Margin="0,10,0,24"/>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- Right Pane: Results -->
        <Grid Grid.Row="1" Grid.Column="1" Padding="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Spacing="4">
                    <TextBlock Text="Total Cooling" Style="{StaticResource CaptionTextBlockStyle}"/>
                    <TextBlock x:Name="ResCooling" Text="0.0 kW" Style="{StaticResource TitleTextBlockStyle}" Foreground="SteelBlue"/>
                    <TextBlock x:Name="ResCoolingBreakdown" Text="S: 0.0 | L: 0.0" FontSize="12" Foreground="Gray"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Spacing="4">
                    <TextBlock Text="Net Reheat Load" Style="{StaticResource CaptionTextBlockStyle}"/>
                    <TextBlock x:Name="ResReheat" Text="0.0 kW" Style="{StaticResource TitleTextBlockStyle}" Foreground="DarkMagenta"/>
                </StackPanel>
                <StackPanel Grid.Column="2" Spacing="4">
                    <TextBlock Text="Total Fan Power" Style="{StaticResource CaptionTextBlockStyle}"/>
                    <TextBlock x:Name="ResFanPower" Text="0.0 kW" Style="{StaticResource TitleTextBlockStyle}"/>
                    <TextBlock x:Name="ResFanBreakdown" Text="S: 0.0 | E: 0.0" FontSize="12" Foreground="Gray"/>
                </StackPanel>
            </Grid>

            <ListView x:Name="ListSchedule" Grid.Row="1" Margin="0,10,0,0">
                <ListView.Header>
                    <Grid Padding="12,8" Background="{ThemeResource LayerFillColorDefaultBrush}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="180"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Process Step" FontWeight="Bold"/>
                        <TextBlock Text="Entering State" Grid.Column="1" FontWeight="Bold"/>
                        <TextBlock Text="Leaving State" Grid.Column="2" FontWeight="Bold"/>
                    </Grid>
                </ListView.Header>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="12,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding Component}" VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding Entering}" Grid.Column="1" VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding Leaving}" Grid.Column="2" VerticalAlignment="Center"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Grid>
</Window>