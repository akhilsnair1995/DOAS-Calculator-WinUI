<Window
    x:Class="DOASCalculatorWinUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DOASCalculatorWinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="420"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left Pane: Inputs -->
        <Border Grid.Column="0" BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}" BorderThickness="0,0,1,0" Padding="0,0,12,0">
            <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="20">
                <StackPanel Spacing="16">
                    <TextBlock Text="Configuration" Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,10"/>

                    <!-- Global Settings -->
                    <Expander Header="System Mode &amp; Site" IsExpanded="True" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch">
                        <StackPanel Spacing="12">
                            <RadioButtons x:Name="RadioMode" Header="Operation Mode">
                                <x:String>Cooling (Summer)</x:String>
                                <x:String>Heating (Winter)</x:String>
                            </RadioButtons>
                            <NumberBox x:Name="NumAltitude" Header="Altitude (m)" Value="0" SpinButtonPlacementMode="Inline"/>
                        </StackPanel>
                    </Expander>

                    <!-- Airflows -->
                    <Expander Header="Airflows &amp; Conditions" IsExpanded="True" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch">
                        <StackPanel Spacing="12">
                            <Grid ColumnDefinitions="*,*">
                                <StackPanel Grid.Column="0" Spacing="8" Margin="0,0,8,0">
                                    <TextBlock Text="Outdoor Air (OA)" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                                    <NumberBox x:Name="NumOaFlow" Header="Flow (L/s)" Value="5000" SpinButtonPlacementMode="Compact"/>
                                    <NumberBox x:Name="NumOaDb" Header="Dry Bulb (°C)" Value="46.0" SpinButtonPlacementMode="Compact"/>
                                    <NumberBox x:Name="NumOaWb" Header="Wet Bulb (°C)" Value="29.0" SpinButtonPlacementMode="Compact"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Spacing="8" Margin="8,0,0,0">
                                    <TextBlock Text="Exhaust Air (EA)" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                                    <NumberBox x:Name="NumEaFlow" Header="Flow (L/s)" Value="4500" SpinButtonPlacementMode="Compact"/>
                                    <NumberBox x:Name="NumEaDb" Header="Dry Bulb (°C)" Value="24.0" SpinButtonPlacementMode="Compact"/>
                                    <NumberBox x:Name="NumEaRh" Header="Rel. Humidity (%)" Value="50.0" SpinButtonPlacementMode="Compact"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Expander>

                    <!-- Energy Recovery -->
                    <Expander Header="Energy Recovery" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch">
                        <StackPanel Spacing="12">
                            <ToggleSwitch x:Name="TogWheel" Header="Enthalpy Wheel" IsOn="True"/>
                            <StackPanel Visibility="{x:Bind TogWheel.IsOn, Mode=OneWay}" Spacing="8" Margin="0,0,0,8">
                                <Grid ColumnDefinitions="*,*">
                                    <NumberBox x:Name="NumWheelSens" Header="Sensible Eff (%)" Value="75" Grid.Column="0" Margin="0,0,4,0"/>
                                    <NumberBox x:Name="NumWheelLat" Header="Latent Eff (%)" Value="70" Grid.Column="1" Margin="4,0,0,0"/>
                                </Grid>
                                <CheckBox x:Name="ChkEconomizer" Content="Economizer Mode (Bypass)" IsChecked="True"/>
                            </StackPanel>
                        </StackPanel>
                    </Expander>
                    
                    <!-- Advanced Recovery -->
                    <Expander Header="Stage 2 Recovery (Wrap-around)" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch">
                        <StackPanel Spacing="12">
                            <ToggleSwitch x:Name="TogDoubleWheel" Header="Series / Double Wheel" IsOn="False"/>
                            <NumberBox x:Name="NumDwEff" Header="Efficiency (%)" Value="60" Visibility="{x:Bind TogDoubleWheel.IsOn, Mode=OneWay}"/>
                            
                            <ToggleSwitch x:Name="TogHp" Header="Heat Pipe" IsOn="False"/>
                            <NumberBox x:Name="NumHpEff" Header="Efficiency (%)" Value="50" Visibility="{x:Bind TogHp.IsOn, Mode=OneWay}"/>
                        </StackPanel>
                    </Expander>

                    <!-- Coils -->
                    <Expander Header="Coils &amp; Reheat" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch">
                        <StackPanel Spacing="12">
                            <NumberBox x:Name="NumOffCoil" Header="Target Off-Coil Temp (°C)" Value="13.0"/>
                            
                            <ToggleSwitch x:Name="TogReheat" Header="Supplementary Reheat" IsOn="False"/>
                            <NumberBox x:Name="NumSupplyTemp" Header="Target Supply Temp (°C)" Value="22.0" Visibility="{x:Bind TogReheat.IsOn, Mode=OneWay}"/>
                        </StackPanel>
                    </Expander>

                    <!-- Fans -->
                    <Expander Header="Fans &amp; Motor" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch">
                        <StackPanel Spacing="12">
                            <Grid ColumnDefinitions="*,*">
                                <NumberBox x:Name="NumSupEsp" Header="Supply ESP (Pa)" Value="250" Grid.Column="0" Margin="0,0,4,0"/>
                                <NumberBox x:Name="NumExtEsp" Header="Extract ESP (Pa)" Value="150" Grid.Column="1" Margin="4,0,0,0"/>
                            </Grid>
                            <NumberBox x:Name="NumFanEff" Header="Fan Efficiency (%)" Value="70"/>
                        </StackPanel>
                    </Expander>
                    
                    <Button x:Name="BtnCalculate" Content="Calculate System" Style="{StaticResource AccentButtonStyle}" HorizontalAlignment="Stretch" Click="Calculate_Click" Margin="0,10,0,20"/>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- Right Pane: Results & Chart -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Results Cards -->
            <StackPanel Grid.Row="0" Background="{ThemeResource LayerFillColorDefaultBrush}" Padding="20">
                <TextBlock Text="System Performance" Style="{StaticResource SubtitleTextBlockStyle}" Margin="0,0,0,12"/>
                <Grid ColumnDefinitions="*,*,*,*">
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Total Cooling" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                        <TextBlock x:Name="ResTotalCooling" Text="0.0 kW" Style="{StaticResource TitleTextBlockStyle}" Foreground="SteelBlue"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="Total Heating" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                        <TextBlock x:Name="ResTotalHeating" Text="0.0 kW" Style="{StaticResource TitleTextBlockStyle}" Foreground="OrangeRed"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Reheat Load" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                        <TextBlock x:Name="ResReheat" Text="0.0 kW" Style="{StaticResource TitleTextBlockStyle}" Foreground="DarkMagenta"/>
                    </StackPanel>
                    <StackPanel Grid.Column="3">
                        <TextBlock Text="Total Fan Power" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                        <TextBlock x:Name="ResFanPower" Text="0.0 kW" Style="{StaticResource TitleTextBlockStyle}"/>
                    </StackPanel>
                </Grid>
            </StackPanel>

            <!-- Chart Area -->
            <Grid Grid.Row="1" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
                 <!-- ScrollViewer allows panning if the chart is zoomed or large -->
                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                    <!-- We wrap Canvas in a container to size it properly -->
                    <Viewbox Stretch="Uniform">
                        <Grid x:Name="ChartContainer" Width="902" Height="652">
                            <Image x:Name="ImgPsychChart" Stretch="Fill"/>
                            <Canvas x:Name="ChartCanvas" Width="902" Height="652"/>
                        </Grid>
                    </Viewbox>
                </ScrollViewer>
            </Grid>
        </Grid>
    </Grid>
</Window>